[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "resilient-token-validation-interview"
version = "0.1.0"
requires-python = ">=3.11"
description = "Senior Engineering Manager coding exercise"

[tool.setuptools.packages.find]
where = ["src"]
